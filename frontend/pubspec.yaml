name: airline_ops
description: Airline Operations System
publish_to: "none"
version: 0.1.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  provider: ^6.0.5
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0
  cached_network_image: ^3.2.3
  flutter_svg: ^2.0.5
  flutter_staggered_grid_view: ^0.6.2
  flutter_spinkit: ^5.1.0
  fluttertoast: ^8.2.1
  flutter_dotenv: ^5.0.2
  flutter_secure_storage: ^5.0.2
  flutter_hooks: ^0.18.6
  hooks_riverpod: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
